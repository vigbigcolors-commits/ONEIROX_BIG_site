---
description: Safe PSEO always — unique pages, DB metrics, top-50 index only, typed/conditional UI
globs: pseo/**,public/somatic/**,public/sitemap*.xml,public/robots.txt
alwaysApply: false
---

# Oneirox Safe PSEO (обязательно)

Programmatic SEO = **архитектура базы метрик**, не генератор SEO-текста.
Сайт уже переживал удаления контента — **нулевой риск thin content**.

## Закон уникальности (строго)

1. Каждая страница — **уникальная композиция** (title / summary / mechanism / decode_hint).
2. Запрещены одинаковые абзацы между URL и copy-paste с открытого веба.
3. Общая оболочка (nav, CTA label, disclaimer) — ок; prose — нет.
4. Билд обязан проходить `npm run pseo:audit` (near-dup → fail).

## Жёсткие правила

1. **Матрица (БД):** 300–1000 плотных строк с метриками + enrichment (`body_zones`, `mechanism_bullets`, `layout_profile`, `modules_present`).
2. **В индекс Google только топ-50** (`indexable: true`).
   - Sitemap somatic = топ-50 + хабы.
   - Остальные: `noindex,follow`, не в sitemap.
3. **Разный DOM** по `layout_profile` / modules (нет данных → нет секции).
4. **Жёсткий CTA** на Decode (`/#decode`).
5. **Запрет:** Фрейд/Юнг/мистика/сонники; одна оболочка на все URL; заливка 1000+ в индекс; forum scrape.

## Команды

```bash
npm run pseo:expand    # DB ~400 + rank top-50
npm run pseo:enrich    # unique composed fields + layouts
npm run pseo:audit     # uniqueness gate
npm run pseo:build     # typed SSG + safe sitemap
npm run pseo:all       # expand → enrich → audit → build
```

## Индексация

Только URL из `pseo/data/indexable-allowlist.json` + `/somatic/` хабы.
